{-| Outcome of a Flight in Progress (FIP). Currently, only `Success` is modelled. -}

module Types.Departure.Outcome (
    Outcome(..)
) where


data Outcome
    = Success
    | Failure
    deriving (Eq, Show)
